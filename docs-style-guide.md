# 文档写作规范

本仓库以中文作为主要叙述语言，同时保留必要的英文工程术语和代码标识。核心术语以 [`terminology.md`](./terminology.md) 为唯一基准；本文件只定义写作规则，不重复维护术语映射。

## 1. 中文负责叙述，英文负责消歧

中文能够准确表达时，正文不使用英文句子或连续英文术语替代中文。

核心概念首次出现时，可采用“中文（English Term）”形式，后续优先使用中文。例如：

- 运行时（Runtime）
- 执行框架（Harness）
- 上下文（Context）
- 工作空间（Workspace）

不要反复写成“中文 + English + 中文 + English”的夹杂句。

## 2. 统一术语必须来自术语表

正文、README、VitePress 首页和架构图中的同一概念必须使用一致译法。

新增或调整核心术语时：

1. 先修改 `terminology.md`。
2. 再同步规范性架构文档和架构图。
3. 最后按需更新研究文章。

不得在单篇文档中自行创造另一套中文译法。

## 3. 工程实体保留英文标识

以下内容可以保留英文：

- 行业缩写和协议：`LLM`、`MCP`、`PTC`、`HITL`、`API`、`SaaS` 等。
- 代码类型名、接口、事件类型、字段名、配置项和命令。
- 目录名、文件名、包名和 Schema 标识。
- 架构图 JSON 中的 `id`、`type`、`from`、`to` 等内部结构字段。

保留英文标识不等于正文必须使用英文叙述。正文应先解释其中文含义。

## 4. 领域模型采用“中文叙述，英文对码”

`Work`、`Session`、`Run`、`Event`、`Action`、`Result`、`Observation`、`Artifact` 等属于正式领域模型实体。

写作时：

- 首次出现使用“中文（English）”。
- 后续正文可以直接使用中文。
- Schema、事件名和代码类型保持英文。
- 架构图展示文本优先中文，必要时保留英文用于与代码模型对应。

例如：

```text
工作（Work）是长期目标和共享工作世界的载体。
一个工作可以包含多个会话，每个会话又可以经历多次运行。
```

而不是：

```text
Work 可以包含多个 Session，每个 Session 可以包含多个 Run。
```

## 5. 架构图同样执行中文优先

架构图不是术语规则的例外。

用户可见的：

- `title` / `subtitle`
- `label` / `sublabel`
- `tag` / `note`
- 卡片内容
- 连接文字

都应遵循 `terminology.md`。

只修改 `.architecture.json` 图源，不直接修改 `public/diagrams/**` 生成文件。内部 ID、类型和连接结构不因中文化而改变。

## 6. 区分文章来源与架构推导

文章笔记必须明确区分：

1. **文章观点**：原文实际支持的内容。
2. **架构分析**：基于文章和讨论进一步形成的抽象、修正与推导。
3. **平台影响**：这些结论会如何改变 Agent Platform 的架构。

不得把我们的推导伪装成文章来源。

引用原文已有术语时，可以在首次出现时保留英文；进入我们的架构分析后，应切回仓库统一术语。

## 7. 一节一个核心问题

每个 Markdown 小节只处理一个核心问题，优先采用以下结构：

```text
# 问题

## 文章中的关键观点
## 我们的架构分析
## 对 Agent Platform 的影响
## 核心结论
```

如果某节内容天然更适合其他结构，可以调整，但应保持单一主题。

## 8. 架构主文档是当前真相

`architecture/agent-platform.md` 是平台当前的规范性架构文档。文章目录保存研究过程和依据，主文档只吸收已经形成的稳定架构结论。

每完成一篇文章：

1. 先提交该文章目录。
2. 再单独提交该文章对 `architecture/agent-platform.md` 带来的增量变化。

这样保留完整的架构演进历史。

## 9. 提交前检查

涉及术语或架构表达的修改至少检查：

- 是否遵守 `terminology.md` 的唯一译法。
- 是否仍存在可以自然翻译却保留的大段英文术语串。
- 是否误翻译了协议、代码标识或 Schema 字段。
- 正文和相关架构图是否使用同一术语。
- 如果修改 `README.md`，是否按仓库规则同步检查并更新 `index.md`。

提交前执行：

```bash
npm run check:terminology
```

该检查扫描当前架构正文和 Archify 图源中的用户可见文本。确有合理例外时，应使用 `terminology-rules.json` 中的显式文件级豁免，而不是删除或放宽全局术语规则。
