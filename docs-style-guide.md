# 文档写作规范

本仓库以中文作为主要叙述语言，同时保留必要的英文工程术语和代码标识。

## 1. 中文优先

中文能够准确表达时，正文不使用英文句子替代中文。重要术语首次出现时采用“中文（English Term）”形式，后续优先使用中文。

例如：

- 执行控制（Execution Control）
- 上下文投影（Context Projection）
- 能力授权（Capability Grant）
- 信任传播（Trust Propagation）
- 工作前沿（Work Frontier）

## 2. 不强行翻译工程实体

以下内容可以保留英文：

- 领域模型实体：`Work`、`Session`、`Run`、`Event`、`Action` 等；正文首次出现时给出中文解释。
- 行业缩写：`LLM`、`MCP`、`PTC`、`HITL`、`API` 等。
- 容易因翻译失真的核心概念：`Harness`、`Skill`、`Tool` 等；正文优先使用“中文解释 + 原词”。
- 目录名、文件名、接口、事件类型、配置项和代码标识符。

## 3. 区分文章来源与架构推导

文章笔记必须明确区分：

1. **文章观点**：原文实际支持的内容。
2. **架构分析**：基于文章和讨论进一步形成的抽象、修正与推导。
3. **平台影响**：这些结论会如何改变 Agent Platform 的架构。

不得把我们的推导伪装成文章来源。

## 4. 一节一个核心问题

每个 Markdown 小节只处理一个核心问题，优先采用以下结构：

```text
# 问题

## 文章中的关键观点
## 我们的架构分析
## 对 Agent Platform 的影响
## 核心结论
```

如果某节的内容天然更适合其他结构，可以调整，但应保持单一主题。

## 5. 架构主文档是当前真相

`architecture/agent-platform.md` 是平台当前的规范性架构文档。文章目录保存研究过程和依据，主文档只吸收已经形成的稳定架构结论。

每完成一篇文章：

1. 先提交该文章目录。
2. 再单独提交该文章对 `architecture/agent-platform.md` 带来的增量变化。

这样保留完整的架构演进历史。
